# Core - required for audio capture
PyAudioWPATCH>=0.2.12
numpy>=1.24

# Transcription - faster-whisper uses CTranslate2 for ~4x faster CPU inference
faster-whisper>=1.1.0

# Web UI
flask>=3.0

# Speaker diarization (optional) - identifies different speakers
# Requires a HuggingFace token: https://huggingface.co/settings/tokens
# Must accept model terms at: https://huggingface.co/pyannote/speaker-diarization-3.1
pyannote.audio>=3.1

# Alternative: openai-whisper (slower, higher memory usage)
# openai-whisper>=20231117
